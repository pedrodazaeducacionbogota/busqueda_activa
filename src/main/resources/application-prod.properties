# ===============================
# PERFIL PROD - Oracle 19C + JNDI (WebLogic)
# ===============================

# DATASOURCE - JNDI (configurado en WebLogic)
spring.datasource.jndi-name=jdbc/SICOBERTURA

# JPA / HIBERNATE
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle12cDialect
spring.jpa.hibernate.ddl-auto=none

# Seguridad habilitada en produccion
app.security.enabled=true

# ===============================
# SWAGGER HABILITADO EN PROD (validación QA SED; deshabilitar al pasar a producción)
# ===============================
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# CORS restringido prod (ZAP hardening 2026-08-03)
cors.allowed-origins=${CORS_ORIGINS:https://sicobertura.educacionbogota.gov.co}
