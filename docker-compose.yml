version: '3.8'

services:
  database:
    image: mysql:8.0
    container_name: mysql-busqueda-activa
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: sed_cobertura
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
